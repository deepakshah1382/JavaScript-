//  this some to the the if eles like only this use for for short hand
//condition ? expressionIfTrue : expressionIfFalse;

let a = 50;
let b = 20;
let number = a > b ? `${a} is big number` : `${b} is the greater number`;
console.log(number);

//  now we chack the age for the voting eligible

let age = 19;
let outp = age >= 18 ? "you cna give voting" : "you not ";
console.log(outp);

//  for chacking the year is leaper or not
let year = 2005;
let ans = year % 4 === 0 ? "leaper year" : "not leaper";
console.log(ans);

// pass or file
let marks = 50;
let result = marks >= 50 ? "pass" : "fail";
console.log(result);

//  chacking let
let char = "Z";
let chacking23 = "AEIOUaeiou".includes(char) ? "vowel" : "constant";
console.log(chacking23);

// smallest number from three
let aa = 10;
let bb = 12;
let cc = 15;
let smallest=a>b?(b>a?c:a)
